[build-system]
requires = ['setuptools>=50.0',"wheel"]
build_backened = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = [
     "tests"
     ]

[tool.mypy]
mypy_path = "src"
ignore_missing_imports = true